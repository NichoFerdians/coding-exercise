/**
  *The class `Car` in TypeScript has properties for registration number and color.
  */
export class Car {
    constructor(
        public registrationNumber: string,
        public color: string
    ) { }
}
